# Apps

Deployable application bundles (composition roots that wire services and
packages together). **Milestone 0: structure only — no apps.**

Apps stay thin: they compose engines and services; business logic lives in the
engines and shared logic in [`../packages`](../packages).

No apps are implemented yet.
