# EIOS 项目总交接文档（读这一份就够）

> 给接手人：这份文档包含项目全部背景、进度、凭证、坑和下一步。
> 从零接手，按本文档做即可。更新时间：2026-08-03。

---

## 一、这是什么项目

**EIOS** = 一个多渠道 AI 消息机器人。用户从 Telegram / 微信 / 企业微信 / WhatsApp
发消息，系统用 AI 处理（对话、总结链接、语音合成），再把回复发回去。

- **代码仓库**：https://github.com/caicanghai/Codex-5.5--
- **工作分支**：`claude/eios-repository-foundation-hoi3u8` ← **重要：代码和文档都在这个分支，不在 main！**
- **真实形态**：单体 FastAPI 应用（`app/` 目录）+ Telegram bot，Docker Compose 编排。
  4 个容器：`db`(Postgres) / `redis` / `api`(端口8000) / `bot`。
- **注意**：`docs/architecture/` 里有 25 份架构文档，那是**未来蓝图**（微服务、多租户等），
  **不是当前代码的现状**。当前代码在 `app/`。别照蓝图去找代码。

---

## 二、⚠️ 先看这个：接手第一步别踩坑

**所有交接文档（本文件、STATUS.md、HANDOFF.md、DEPLOYMENT.md）和修好的部署脚本，
都在 `claude/eios-repository-foundation-hoi3u8` 分支上，不在 `main`。**

拉代码必须切分支，否则看不到这些文件：

```bash
git clone https://github.com/caicanghai/Codex-5.5--.git
cd Codex-5.5--
git checkout claude/eios-repository-foundation-hoi3u8   # ← 必须！
ls 交接总纲.md STATUS.md HANDOFF.md DEPLOYMENT.md         # 确认能看到
```

之前有人接手"GitHub 失败"，八成就是没切分支，停在 main 上看不到文件。

---

## 三、硬件 / 账号 / 资源（已确认）

| 项 | 值 |
|----|----|
| 服务器 | 阿里云 ECS，**中国香港** B 区 |
| 公网 IP | `8.217.110.255` |
| 规格 | Ubuntu 22.04，2核 16G，40G 盘，已装 Docker |
| 登录方式 | 阿里云控制台网页「远程连接」（不用自己配 ssh） |
| 域名（3个，**都未备案**） | `trip-king.top` / `tripsking.art` / `tripsking.top` |
| 用户身份 | 尤咏慈 / 业务实体「尤太帝」 |

---

## 四、⚠️ 三条铁律（接手人必须知道，否则白干）

1. **AI 助手无法 SSH 进这台服务器**（沙箱网络限制）。
   → 所有服务器命令，需要**真人在阿里云网页「远程连接」里手动执行**。AI 只能给命令、看日志判错。

2. **备案是硬门槛，任何技术手段都绕不过**。
   → 企业微信「收消息」、微信公众号，腾讯强制要求 **ICP 备案域名**（备案主体=企业主体）。
   ngrok、Cloudflare 隧道都没用（它校验的是备案，不是能否访问）。域名备案要 2-3 周。

3. **个人微信登录服务器 IP（尤其香港）极易被封号**。
   → 用 OpenClaw 接微信时，把个人微信登在阿里云香港服务器上，微信风控几乎必封。
   要么把 OpenClaw 留在本地 Windows 电脑跑，要么用小号，别用主号。

---

## 五、五个渠道的完整状态 + 下一步

### ✅ 渠道1：Telegram —— 零门槛，最优先跑通
- 代码：完成（`app/bot/`）
- 缺：`TELEGRAM_BOT_TOKEN`（找 @BotFather 建 bot 拿）+ `TELEGRAM_OWNER_ID`（找 @userinfobot 拿自己ID）
- **下一步**：填进服务器 `.env` → 部署（见第六节）→ 给 bot 发消息测试
- 不需要域名、不需要备案、双向对话+语音全支持

### 🟡 渠道2：WhatsApp —— 做了一半，差 token
**已完成**：
- WhatsApp 业务账户（WABA）已建，状态"审核中"（正常）
  - **WABA ID（BUSINESS_ACCOUNT_ID）= `2448528952314302`**
- 已加手机号：柬埔寨 `+855 96 761 1765`
  - **PHONE_NUMBER_ID = `1238587366009579`**
- 开发者 App 已建：名「稍安勿躁」，**App ID = `2218017788931551`**，开发模式

**还差**：
- ⚠️ App 里加错产品了：加成了「Facebook 登录」，需**改加 WhatsApp 产品**
- ⬜ `WHATSAPP_ACCESS_TOKEN`
- ⬜ `WHATSAPP_APP_SECRET`
- ⬜ `WHATSAPP_VERIFY_TOKEN`（自己编一个，如 `eios_verify_2026`）

**下一步（具体操作）**：
1. 打开 https://developers.facebook.com/apps → 点进「稍安勿躁」App
2. 面板找「添加产品」→ 加 **WhatsApp**（不是 Facebook 登录）→ 关联业务组合
3. 左侧 WhatsApp → **API 设置**：复制「临时访问令牌」= ACCESS_TOKEN；页面也有 Phone ID / WABA ID
4. 应用设置 → 基本 → **应用密钥** = APP_SECRET
5. 5 个值填服务器 `.env`（见第七节）
6. Webhook 回调填 `https://tripsking.top/webhook/whatsapp`（WhatsApp **不需要备案**，配 Caddy HTTPS 即可，见第六节）
7. ⚠️ 临时 token 只有 24 小时。正式用要去 business.facebook.com → 业务设置 → 系统用户 → 生成**永久 token**

### 🟡 渠道3：微信（走 OpenClaw）—— 卡 Node 版本
- **OpenClaw 是绕过备案接微信的方案**（EIOS 主动连它，像 Telegram，不用域名）
- 现状：OpenClaw 装在**用户的 Windows 电脑**（主机名 DESKTOP-MBS7J1P），不在服务器
- 问题：OpenClaw 需 **Node.js v22.19+**，但装的是 v22.14.0（太老）、系统跑 v24.15.0（太新，不认）→
  `openclaw status` 失败，微信里显示"暂无法连接 OpenClaw"
- **还需要**：一个**活的 OpenClaw 服务地址 + API Key**（EIOS 的 `.env` 里填 `OPENCLAW_BASE_URL` / `OPENCLAW_API_KEY`）
- **待决策**：微信放哪跑？
  - 留 Windows（号安全）：装 Node v22.19，跑起 OpenClaw gateway，内网穿透连回服务器
  - 上阿里云（用户想全上云）：⚠️ 封号风险高，只能用小号
- **诊断第一步**：在 Windows 上跑这三条，拿真实报错：
  ```
  node -v
  & "C:\node-v22.14.0-win-x64\node_modules\.bin\openclaw" --version
  & "C:\node-v22.14.0-win-x64\node_modules\.bin\openclaw" status
  ```
- ⚠️ OpenClaw 桥接个人微信，违反微信条款，有封号风险，别用于正经业务

### 🟢 渠道4：企业微信-推送 —— 能做，不用域名
- 只主动推送通知给员工，**不需要回调、不需要域名、不需要备案**
- 已知 CorpID：`ww0b1b3e2c9a997135`
- **还差**：AgentID + Secret（企业微信后台 → 应用管理 → 你的应用里拿）
- **下一步**：填 `.env` 的 `WECOM_CORP_ID/AGENT_ID/SECRET`，`WECOM_ENABLED=true`，
  **不填** TOKEN/AESKey（那俩是"收消息"才要的）

### 🔴 渠道5：企业微信-收消息 / 微信公众号 —— 卡备案
- 都需要备案域名做回调，**无法绕过**，等域名备案（2-3周）
- ⚠️ 用户之前在聊天里截图暴露过企业微信的 Token/AESKey，**建议后台"随机获取"重新生成**，旧的当泄露作废

---

## 六、怎么部署（真人在阿里云远程连接里执行）

```bash
# 1. 拉代码（注意切分支）
cd /opt
git clone https://github.com/caicanghai/Codex-5.5--.git
cd Codex-5.5--
git checkout claude/eios-repository-foundation-hoi3u8

# 2. 配置（用根目录的 .env.example，不是 deployments/ 里的）
cp .env.example .env
nano .env          # 至少填 TELEGRAM_BOT_TOKEN 和 TELEGRAM_OWNER_ID

# 3. 启动
docker compose up -d --build      # 老版本命令：docker-compose up -d --build

# 4. 验证
docker compose ps                 # db/redis/api/bot 四个都要 Up
curl http://localhost:8000/health
docker compose logs -f bot        # 看 Telegram 是否连上
```

### 给 WhatsApp 配 HTTPS（有域名，不用备案）
```bash
# 域名 tripsking.top 的 A 记录解析到 8.217.110.255
# 根目录建 Caddyfile：
#   tripsking.top {
#       reverse_proxy localhost:8000
#   }
# 跑 Caddy，自动申请 Let's Encrypt 证书
# 回调 URL：https://tripsking.top/webhook/whatsapp
```

---

## 七、所有凭证清单（填服务器 `.env`）

### 已拿到（非敏感，可用）
```
VPS IP                       = 8.217.110.255
WHATSAPP_BUSINESS_ACCOUNT_ID = 2448528952314302
WHATSAPP_PHONE_NUMBER_ID     = 1238587366009579
WhatsApp 手机号               = +855 96 761 1765
Meta App ID                  = 2218017788931551 （名：稍安勿躁）
WECOM_CORP_ID                = ww0b1b3e2c9a997135
域名                          = trip-king.top / tripsking.art / tripsking.top（未备案）
```

### 还没拿到（敏感，拿到后只填服务器 `.env`，别贴聊天/截图）
```
TELEGRAM_BOT_TOKEN     = ⬜ @BotFather
TELEGRAM_OWNER_ID      = ⬜ @userinfobot
WHATSAPP_ACCESS_TOKEN  = ⬜ Meta App → WhatsApp → API设置
WHATSAPP_APP_SECRET    = ⬜ Meta App → 设置 → 基本
WHATSAPP_VERIFY_TOKEN  = ⬜ 自己编，如 eios_verify_2026
WECOM_AGENT_ID         = ⬜ 企业微信后台 → 应用
WECOM_SECRET           = ⬜ 企业微信后台 → 应用
```

### `.env` 示例（各渠道打开方式）
```env
# Telegram（现在就能跑）
TELEGRAM_BOT_TOKEN=
TELEGRAM_OWNER_ID=
CHANNELS_ENABLED=telegram

# AI（可选，留空用离线摘要）
AI_API_KEY=            # OpenRouter 免费key: openrouter.ai
AI_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=llama-3.1-8b

# WhatsApp（拿到凭证后）
WHATSAPP_ENABLED=true
WHATSAPP_BUSINESS_ACCOUNT_ID=2448528952314302
WHATSAPP_PHONE_NUMBER_ID=1238587366009579
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_VERIFY_TOKEN=eios_verify_2026

# 企业微信-只推送（拿到 AgentID/Secret 后）
WECOM_ENABLED=true
WECOM_CORP_ID=ww0b1b3e2c9a997135
WECOM_AGENT_ID=
WECOM_SECRET=

# 数据库密码（建议改强密码）
POSTGRES_PASSWORD=换成强密码
```

---

## 八、执行优先级（建议顺序）

1. **【最高】部署 + 跑通 Telegram** —— 零门槛，先验证整个系统能跑
2. **【高】WhatsApp 拿 token** —— 只差 App 加 WhatsApp 产品 + 拿 token；域名已有
3. **【中】企业微信-推送** —— 拿 AgentID/Secret 即可，不用域名
4. **【待决策】微信-OpenClaw** —— 先出真实报错，决定放 Windows 还是服务器（封号风险）
5. **【阻塞】企业微信双向 / 公众号** —— 等域名备案，无法提前

---

## 九、阻塞项一览

| 阻塞 | 卡住谁 | 解法 | 时间 |
|------|--------|------|------|
| 域名未备案 | 企微双向、公众号 | 阿里云备案 | 2-3周 |
| WhatsApp 没 token | WhatsApp | App 加 WhatsApp 产品拿 token | 分钟级 |
| OpenClaw Node 版本 | 微信 | 装 Node v22.19 | 分钟级 |
| OpenClaw 无服务地址 | 微信 | 自建 openclaw 服务 | 待定 |
| AI 无法 SSH | 全部部署 | 真人在阿里云远程连接执行 | —— |
| 微信服务器封号风险 | 微信上云 | 用小号 或 留 Windows | 决策 |

---

## 十、代码位置索引（接手人快速定位）

| 想改什么 | 看哪个文件 |
|---------|-----------|
| 消息路由（对话/总结/工作流） | `app/dispatch.py` |
| AI 对话 + 故障转移 | `app/pipeline/chat.py` |
| 各渠道发送 | `app/messaging/<渠道>.py` |
| webhook 接收 | `app/api/main.py`（/health、/ingest、/webhook/*） |
| 微信/企微加解密 | `app/messaging/wxcrypt.py` |
| 语音合成 | `app/voice/` |
| 全部配置项 | `app/config.py` + `.env.example` |
| 部署编排 | `docker-compose.yml`（根目录） |

---

## 十一、其它交接文档（同一分支，可交叉参考）

- `STATUS.md` —— 简版进度+待办
- `HANDOFF.md` —— 项目全貌+渠道状态表
- `DEPLOYMENT.md` —— 详细部署步骤
- `PROJECT_SUMMARY.md` —— 项目概览（顶部标注了哪些是愿景）
- `docs/architecture/` —— 未来架构蓝图（25份，设计目标非现状）

---

## 一句话总结

代码 5 渠道都写好了。**先部署跑通 Telegram**（零门槛）；WhatsApp 差最后一步拿 token
（域名已有，不用备案）；企业微信能"只推送"；微信走 OpenClaw 但卡 Node 版本+封号风险；
企业微信双向和公众号卡备案（2-3周）。真人在阿里云远程连接里执行命令，AI 给命令判错。
