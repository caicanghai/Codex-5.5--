// EIOS build placeholder (Milestone 0).
// No buildable source exists yet; this succeeds so CI has a green `build` stage.
// Replaced by a real workspace build in a later milestone.
console.log('EIOS: nothing to build yet (Milestone 0). See docs/ROADMAP.md.');
process.exit(0);
