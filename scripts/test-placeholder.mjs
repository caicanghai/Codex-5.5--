// EIOS test runner placeholder (Milestone 0).
// No tests are required yet; this succeeds so CI has a green `test` stage.
// Replaced by a real test runner (e.g. vitest) in a later milestone.
console.log('EIOS: no tests defined yet (Milestone 0). See tests/README.md.');
process.exit(0);
